# Planning Version Note

The repository currently preserves the user-provided v0.6 planning document as Markdown. Project history contains references to later planning revisions, so the planning file is identified as the currently available original baseline rather than asserted to be the final historical revision. A later verified original may supersede it without altering the v4.4 database snapshot provenance.
