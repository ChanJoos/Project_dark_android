# Migration Record — 2026-09-10

The user-provided `PROJECT_DARK_Master_DB_v4.4_VISUAL_MANIFEST.xlsx` was converted and preserved in Git as `PROJECT_DARK_MASTER_DB.tar.gz`. The conversion snapshot contains 92 CSV sheets plus the conversion manifest and planning Markdown. This migration exists to make the database durable and reproducible in the repository even when direct workbook editing is unavailable to automated agents.
